# ColorTrack — план за имплементация

Документът следва приоритета от разговора. Отбелязвай завършените стъпки (`[x]` / `[ ]`).

---

## Фаза 1 — Клиенти (CRUD основа) — **готово**

| Стъпка | Статус |
|--------|--------|
| `POST /api/clients` — създаване | [x] |
| `PATCH /api/clients/:id` — редакция | [x] |
| `apiPatch` в `src/api/client.js` | [x] |
| Екран `ClientForm` (нов + редакция) | [x] |
| Бутон `+` в Clients → форма | [x] |
| Бутон Edit в Client detail → форма | [x] |
| Презареждане на списъка клиенти при focus | [x] |

_(Фази по-долу: 1–3 завършени.)_

---

## Фаза 2 — Срещи (appointments) — **готово**

| Стъпка | Статус |
|--------|--------|
| `POST /api/appointments` | [x] |
| `PATCH /api/appointments/:id` | [x] |
| `DELETE /api/appointments/:id` (или отмяна с флаг) | [x] |
| UI: нова/редакция на среща от календар | [x] |
| Тап от началния екран към календар/клиент | [x] |

---

## Фаза 3 — Начален екран (Home) — **готово**

| Стъпка | Статус |
|--------|--------|
| Интерактивна дата лента + данни за избран ден | [x] |
| Търсене (клиенти по име / телефон / имейл) | [x] |
| Картата Upcoming → навигация | [x] |
| Картата Low stock → Inventory | [x] |

_(Фаза 4+ по плана.)_

---

## Фаза 4 — Склад (Inventory)

| Стъпка | Статус |
|--------|--------|
| `GET /api/inventory/:id` | [x] |
| `PATCH /api/inventory/:id` (количество, праг) | [x] |
| `GET /api/inventory/:id/movements` | [x] |
| UI: екран за количество / праг + история | [x] |
| Document / mail бутоните — премахнати | [x] |

---

## Фаза 5 — Досие (допълнения)

| Стъпка | Статус |
|--------|--------|
| Снимки: модел + API + качване | [ ] |
| Детайл на едно посещение (отделен екран) | [ ] |
| Запис „patch test“ от приложението | [ ] |

---

## Фаза 6 — Продукт (по желание)

- Автентикация (роли, салон)
- Push известия
- Офлайн режим
- Връзка appointment ↔ visit (среща изпълнена → посещение)

---

_Последна актуализация: фаза 4 — inventory detail + PATCH + movements, списък с refresh при focus._