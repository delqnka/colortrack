{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "npm install && cd api && npm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index?vpath=$1" }],
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "includeFiles": "api/**"
    }
  }
}
